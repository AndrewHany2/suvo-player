module.exports = {
  presets: ['babel-preset-expo'],
  // @tamagui/babel-plugin enables compile-time optimization.
  // Enable for production; requires 'typescript' devDep and adds build time.
  // plugins: [['@tamagui/babel-plugin', { config: './src/tamagui.config.js', components: ['tamagui'] }]],
};
